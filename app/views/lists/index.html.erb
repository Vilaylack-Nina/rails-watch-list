<div class="container">
  <h1>My Watch Lists</h1>
  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-4">
        <div class="card p-3 mb-3">
          <h2><%= list.name %></h2>
          <%= link_to "See List", list_path(list), class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
